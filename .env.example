# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_APP_NAME="Rishi Platform"
NEXT_PUBLIC_APP_VERSION="1.0.0"

# Environment Control (for database selection)
# Set to 'staging' for staging deployment, 'production' for production
DEPLOY_ENV=development
STAGING=false

# Database Configuration - Environment Specific
# Development Database (Replit built-in Neon database)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# Staging Database (Staging Neon database) 
STAGING_DATABASE_URL="postgresql://neondb_owner:npg_UgTA70PJweka@ep-jolly-cherry-a8pw3fqw-pooler.eastus2.azure.neon.tech/rishiapp_staging?sslmode=require&channel_binding=require"

# Production Database (Production Neon database)
PRODUCTION_DATABASE_URL="postgresql://neondb_owner:npg_UgTA70PJweka@ep-jolly-cherry-a8pw3fqw-pooler.eastus2.azure.neon.tech/rishiapp_prod?sslmode=require&channel_binding=require"

# Legacy PostgreSQL configuration (for compatibility)
PGUSER="database_username"
PGPASSWORD="database_password"
PGHOST="database_host"
PGPORT=5432
PGDATABASE="database_name"

# Authentication Configuration
JWT_SECRET="your-secure-jwt-secret-here"
JWT_REFRESH_SECRET="your-secure-jwt-refresh-secret-here"

# Feature Flags
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_MONITORING=false
NEXT_PUBLIC_ENABLE_DEBUG=true

# Development Settings
DEBUG_DOCS=false
ENABLE_MOCK_DATA=false

# Optional: Google Maps API (for location features)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# Optional: Analytics
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID="your-google-analytics-id"

# Optional: Error Tracking
NEXT_PUBLIC_SENTRY_DSN="your-sentry-dsn"