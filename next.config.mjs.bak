/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { 
    unoptimized: true 
  },
  experimental: { 
    typedRoutes: true 
  },
  trailingSlash: true,
  skipMiddlewareUrlNormalize: true,
};

export default nextConfig;