{
  "appId": "com.rishi.platform",
  "appName": "Rishi Platform",
  "framework": "capacitor",
  "verbose": true,
  "prebuild": "npm run build",
  "build": {
    "scripts": {
      "build": "cp next.config.voltbuilder.mjs next.config.mjs && cp package.voltbuilder.json package.json && npm install && next build"
    }
  }
}